language: php
php:
  - 7
addons:
  apt_packages:
      - php-curl-7.46.0
      - php-openssl-1.0.2j
before_script:
  - openssl version -v
  - curl -V
  - composer self-update
  - composer update --no-interaction --prefer-dist
script:
  - vendor/bin/phpcs module/